node {
   stage ('Scm Checkout'){
      git 'https://github.com/pampanoor/my-app'
   }
   stage('Maven Package'){
      // Get maven home path def mvnHome = /usr/share/maven
      sh "${mvnHome}/bin/mvn package"
   }
}
